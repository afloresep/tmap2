{#
  Server template compatibility alias.

  The unified visualization shell now lives in base.html.j2 and supports both:
  - inline mode (to_html / write_html)
  - fetch mode (write_static / serve)

  Keeping server.html.j2 as an alias preserves backwards compatibility for
  existing code that still references template_name='server.html.j2'.
#}
{% extends "base.html.j2" %}
