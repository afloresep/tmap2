{#
  SMILES template compatibility alias.

  SMILES rendering is built into base.html.j2 when metadata.smilesColumn is
  present. This alias is kept so existing calls to
  template_name='smiles.html.j2' continue to work.
#}
{% extends "base.html.j2" %}
